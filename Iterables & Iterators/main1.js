const str = "Watching Naruto";

for(const ch of str){//iterates on values
    console.log(ch);
}

for(const ind in str){//iterates on index
    console.log(ind);
}

const arr = ["Hello","I","am","Lakshit","I","love","coding"];

for(const wrd of arr){//iterates on values
    console.log(wrd);
}

for(const ind in arr){//iterates on index
    console.log(ind);
}